# --- Core dependencies ---
# PyTorch (CPU-only default)
torch>=2.2,<3.0

# For GPU users (uncomment the next line for CUDA 12.1, or replace with your CUDA version)
# torch --index-url https://download.pytorch.org/whl/cu121

dgl==1.1.2 # required by TxGNN
pandas==1.5.3 # TxGNN uses DataFrame.append(), removed in pandas>=2.0
numpy>=1.23.0,<2.0

# --- TxGNN (Harvard MIMS GitHub) ---
git+https://github.com/mims-harvard/TxGNN.git

# --- Data processing & analysis ---
polars>=1.0
networkx>=3.0
matplotlib>=3.7

# --- Optional: Jupyter environment for notebooks ---
notebook
